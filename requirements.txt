# 🚀 Klaro Educational Platform - Working Requirements for Python 3.12
# Minimal dependencies that actually work on Railway

# Core FastAPI & Server
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# AI Integration
openai==1.40.0
tenacity==8.2.3

# Database
psycopg2-binary==2.9.9
python-dotenv==1.0.0

# Supabase (minimal version)
supabase==2.7.4
# WebSockets runtime for Supabase (pin to v12 to avoid incompatibilities)
websockets==12.0

# Basic utilities
requests==2.31.0
pydantic==2.5.0

# Additional dependencies (Python 3.12 compatible)
aiohttp==3.9.1
Pillow==10.4.0
